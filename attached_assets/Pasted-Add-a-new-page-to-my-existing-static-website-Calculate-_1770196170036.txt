Add a new page to my existing static website Calculate 360 called Body Fat % Estimator (US Navy Method). It must match the same design system, navigation, and responsive layout used across my other calculators (minimal white theme, cards, rounded corners, subtle shadows). Everything must be fully client-side (no backend, no external libraries).

1) Files to create / update

Create:

body-fat-estimator.html

body-fat.js (preferred) OR integrate into shared JS architecture

Update:

Global navigation across all pages: add Body Fat % Estimator

calculators.html hub page: add a new card for this calculator

2) Calculator method (must)

Implement the US Navy Body Fat % method with separate input forms for:

Male

Female

Provide a gender segmented control at the top (default Male). Switching should show only relevant fields.

3) Units + inputs (must)

Add unit toggle:

Metric (default): cm, kg (kg optional)

Imperial: inches, lbs (lbs optional)

Required measurements

Male:

Height

Neck circumference

Waist circumference

Female:

Height

Neck circumference

Waist circumference

Hip circumference

Optional:

Weight (to compute fat mass & lean mass; optional but recommended)

Add helper text:

“Measure circumferences snug, not tight; use consistent posture.”

For waist: around navel (men) / narrowest point (women) or provide clear instruction—pick one and be consistent.

4) Calculations (must)

Use the standard US Navy formulas (log10 based). Convert to inches if needed, then apply:

Male (in inches):

BF% = 86.010 × log10(waist − neck) − 70.041 × log10(height) + 36.76

Female (in inches):

BF% = 163.205 × log10(waist + hip − neck) − 97.684 × log10(height) − 78.387

Rules:

Ensure (waist − neck) > 0 for male; (waist + hip − neck) > 0 for female; otherwise show inline error.

Show calculations in real time.

Display BF% rounded to 1 decimal (selector: 1 / 2 decimals).

If Weight is provided:

Fat Mass = weight × BF%

Lean Mass = weight − fat mass
(Use kg or lb based on selected unit, and label clearly.)

5) Outputs (must)

Show result cards:

Estimated Body Fat %

Body fat category (basic, non-medical) with general ranges:

Provide categories for Male/Female (Athlete/Fitness/Average/High) with approximate ranges

Include disclaimer: “Ranges vary by source; this is a general guide.”

Also show:

Measurement summary (height, neck, waist, hip if applicable)

Optional fat mass / lean mass if weight entered

6) Visual indicator (must)

Add a simple range bar/gauge (no external libs) for body fat %:

Segments for Athlete / Fitness / Average / High

Marker showing current BF%

Gender-specific ranges (approx; keep them configurable in code).

7) Validation + UX (must)

Inline errors only (no alerts)

Empty inputs show “—”

Prevent negative values

Height must be > 0

Circumferences must be > 0

If inputs incomplete, don’t compute; show placeholders

Add buttons:

Reset

Copy Result (BF% + inputs)

Copy Share Link (query params like ?unit=metric&gender=male&h=175&neck=38&waist=85&weight=70)

8) Content + SEO (must)

In body-fat-estimator.html:

Title: “Calculate 360 – Body Fat % Estimator (US Navy Method)”

Meta description: “Estimate your body fat percentage using the US Navy method with simple measurements. Optional fat and lean mass breakdown. Calculate 360.”

Open Graph tags

Add “How to measure” section with bullet instructions (neck/waist/hip)

Add FAQs + FAQ schema (JSON-LD):

Is the US Navy method accurate?

Where to measure waist/hip?

Why does it differ from smart scales?

Can I use it for children? (answer: not intended)

Include disclaimer:

“This is an estimate, not medical advice.”

9) Monetization placeholders

Add ad placeholders (no real AdSense code):

“Ad slot (Top)”

“Ad slot (Sidebar on desktop)”

“Ad slot (Bottom)”
Layout must not break if empty.

10) Final output required

Provide complete working code for:

body-fat-estimator.html

body-fat.js

Any required updates to styles.css

Updates to calculators.html (hub card)

Updates to navigation across all pages

Everything must work as a deployable static website with no external libraries.